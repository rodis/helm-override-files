apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: awx-secret-key
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: doppler-token-secret
    namespace: doppler-operator-system
  project: awx-admin-secret-key
  config: prd
  nameTransformer: lower-snake
  secrets:
    - SECRET_KEY
  managedSecret:
    name: awx-secret-key
    namespace: awx
    type: Opaque