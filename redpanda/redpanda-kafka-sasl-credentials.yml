apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: redpanda-kafka-sasl-credentials
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: doppler-token-secret
    namespace: doppler-operator-system
  project: redpdanda-kafka-sasl-credentials
  config: prd
  managedSecret:
    name: kafka-sasl-credentials
    namespace: redpanda
    type: Opaque
