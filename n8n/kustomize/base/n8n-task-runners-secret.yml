apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: n8n-task-runners-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: doppler-token-secret
    namespace: doppler-operator-system
  project: n8n-runners-auth-token
  config: prd
  nameTransformer: lower-kebab
  managedSecret:
    name: n8n-task-runners-secret
    namespace: n8n
    type: Opaque