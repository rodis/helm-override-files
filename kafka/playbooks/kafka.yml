- hosts: localhost
  become: false
  gather_facts: no
  tasks:
    - name: Create a Kafka namespace
      kubernetes.core.k8s:
        name: kafka
        api_version: v1
        kind: Namespace
        state: present
    - name: Install kafka credential secret
      kubernetes.core.k8s:
        state: present
        src: ../kafka-sasl-credentials.yml
        apply: true