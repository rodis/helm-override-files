apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: inference-redis-credentials
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: doppler-token-secret
    namespace: doppler-operator-system
  project: inference-redis
  config: prd
  managedSecret:
    name: redis-credentials
    namespace: inference
    type: Opaque
